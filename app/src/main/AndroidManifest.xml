<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <!-- External workspace: the bash process needs access to user-picked
       directories (Android 11+ scoped storage). -->
  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <!-- Keep-alive (L1): restart the service after boot / upgrade / power
       reconnect / screen unlock. -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <!-- Keep-alive (L3): user-facing battery-optimization exemption prompt. -->
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

  <application
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:theme="@style/Theme.Dsh"
    android:networkSecurityConfig="@xml/network_security_config">
    <!-- Accessibility: TalkBack support metadata -->
    <meta-data
        android:name="android.accessibilityservice.AccessibilityService"
        android:resource="@xml/accessibility_config" />

    <service
      android:name=".EngineService"
      android:exported="false"
      android:foregroundServiceType="dataSync" />

    <!-- Keep-alive (L1): boot / upgrade / power / unlock re-raise the
         foreground service (Android 12+ exempts these broadcasts from the
         background-start restriction). -->
    <receiver
      android:name=".BootReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
        <action android:name="android.intent.action.USER_PRESENT" />
      </intent-filter>
    </receiver>

    <!-- Keep-alive (L2): heartbeat recovery alarm (self-re-arming). -->
    <receiver
      android:name=".KeepAliveAlarmReceiver"
      android:exported="false" />

    <!-- Shizuku keep-alive bridge: must be declared and exported for the
         Shizuku service to deliver the binder (dev.rikka.shizuku:provider). -->
    <provider
      android:name="rikka.shizuku.ShizukuProvider"
      android:authorities="${applicationId}.shizuku"
      android:exported="true"
      android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
      android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
